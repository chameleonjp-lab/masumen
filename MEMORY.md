# Grid Signal Arena — 制作メモ

- 指定ページは『ロックマンエグゼ4』の戦闘画面説明であり、開始時のランダム5枚選択、同名または同コードの複数選択、3×3領域の移動、通常・溜め射撃、チップ使用、再カスタム、カウンター、心理状態、結果画面を説明している。
- 本作は上記の**抽象的な戦闘形式**だけを参考にし、名称、登場人物、画像、固有のチップやUIを使用しない。
- アートの核は「Signal Relay Tactical」。深いグラファイト、ティール、オーカー、アイボリー、Relay Ember #FF5E3Bを使用し、角を切った端末パネルとリレートレーサーを反復する。
- 生成アセットはバックグラウンドで置き換え完了するため、予約済みの `/manus-storage/` URLを直接実装に組み込む。

## 視覚検証メモ

- デスクトップ初期画面は、生成した参照画を低彩度の背景に使いつつ、手札・カード名・レーン・出力・決定操作が明快に読める。
- `?demo` の端末HUDは更新されているが、初回キャプチャではBabylonの盤面・ユニットが画面に描画されず、中央が暗いままだった。次の作業ではアクティブカメラ・描画エンジン・ランタイムログを優先して確認する。
- アクティブカメラの設定後、デスクトップの自動戦闘ではティール／オーカー領域の6×3盤面、分断線、プレイヤー、ドローン、警告タイルが正しく描画された。
- 390×844のカスタム画面は横スクロールの手札と決定ボタンが読める。一方で同サイズの初回自動戦闘キャプチャには盤面が写らなかったため、ポートレート表示でのカメラ適応または描画開始時機を追加検証する。
- ポートレート時はカメラ距離と画角を広げる調整を追加し、最終キャプチャでプレイヤー、敵、領域境界、予兆マス、タッチ操作を同一画面内に表示できた。

## 連戦拡張メモ

- ユーザー提供のモバイル画面から、キャラクター画像が上下逆転し、画面上の上下と移動操作の対応も反対であることを確認した。
- 全ユニットのテクスチャV座標を反転し、キーボードとタッチパッドの上下移動を画面上の盤面方向へ合わせた。デスクトップとポートレートの両方でプレイヤーと敵が正立することを確認した。
- Wave 1から4を順に攻略する連戦、追加3種の敵、12種の信号カード、ウェーブ間の耐久回復、ブラウザ内のハイスコア／到達ウェーブ保存を実装した。
- 修正後のデスクトップと390×844のモバイルキャプチャで、ユニットが正立し、盤面上の上方向と上ボタンの操作方向が一致する構成になったことを確認した。
- `?demo&wave=2` ではRAZOR-6、MORTAR-NODE、SCANNER-8を、`?demo&wave=3` ではVOLT-SENTINELを含む3体編成を確認した。いずれも敵分析パネル、盤面、予兆表示と整合している。

## 敵AI拡張メモ

- BULWARK-3は横一列をロックするLANE SWEEP、SCANNER-8は縦一列をロックするCOLUMN SCAN、RAZOR-6はプレイヤーの行へ詰めるPURSUIT DASH、MORTAR-NODEは三点へ着弾するMORTAR SPREAD、VOLT-SENTINELは交互マスを塞ぐPULSE GRIDを持つ。
- 並列キャプチャではWave 1で盤面と状態ラベルを確認できた一方、Wave 2・3は起動直後の描画中フレームだったため盤面が欠けた。次の検証では各Waveを単独で再キャプチャし、危険マスと盤面の同時表示を確認する。
- 単独キャプチャではWave 2のMORTAR SPREADで三マスの危険範囲、Wave 3のPULSE GRIDで交互五マスの危険範囲を確認した。RAZOR-6はプレイヤー行へ進出し、敵分析パネルには各固有パターンが表示されている。

## 予兆演出メモ

- 危険マスには朱色の発光に加え、拡大する走査ディスクと収束リングを配置した。予兆時間の経過につれて点滅周期と輝度が上がり、攻撃直前の危険度を伝える。
- Wave 3の複数危険マスでデスクトップ・390×844モバイルともにリングと走査ディスクを確認した。`prefers-reduced-motion`では点滅速度を固定し、静的な高コントラスト表示を維持する。

## バトルチップ型カードメモ

- カスタム画面を日本語名カードとコード表示へ更新した。初期手札では連射弾・貫通槍・追尾弾がコードA/Bで並び、同名または同コードで連結する意図を画面上で確認できた。
- Wave 3自動戦闘では、新しい「NEXT CARD」枠、カスタムゲージ、敵固有予兆が同一画面に表示された。自動操作では5秒時点でキューを使い切っていたため、次の検証ではメガ枠を含むWave 4開始画面とモバイルのカード表示を個別確認する。
- `?wave=4` では、通常カード4枚と紫色で識別されたメガ枠「夢幻障壁」が同時に出現することを確認した。コードAの同名「炎脈弾」も並び、複数選択ルールの実例になる。
- 390×844ではカードデッキが横スクロールとなり、カード名・コード・出力・決定ボタンは読み取れる。メガ枠は右側にあり、スワイプで到達できる構成を維持している。
- 最終ドローでは、同名かつコードCの「拘束杭」が2枚、通常枠、メガ枠「夢幻障壁」が同じ5枚に出現した。複数選択の同名条件と希少枠の視認性を確認できた。
- Wave 4自動戦闘の初回キャプチャは、HUDのみが先に描画される起動中フレームだった。既存の単独Wave検証で盤面描画は確認済みのため、カード実行の確認はコード型検査と手札画面・敵予兆の個別キャプチャを組み合わせて行う。

## カード演出・効果音メモ

- カード系統・状態・メガ枠をイベントとして場面へ送信し、収束リング、拡散ディスク、火花、多重ハローを短寿命のBabylonメッシュで表示する構成にした。音声はWeb Audio APIによる合成のため、外部音源・収集要素を持たずランごとに破棄される。
- Wave 4のデスクトップ自動戦闘では、危険マスの朱色予兆とカード用の光輪が盤面上で混同せず、画面右上のSFXトグルも確認できた。390×844ではトグルが上端のHUD内に収まり、タッチ操作・敵情報・盤面を遮らない。

## 戦闘フィードバック強化メモ

- 敵撃破時は、BULWARK-3／MORTAR-NODEに重い破砕波、SCANNER-8に走査リング、RAZOR-6にスパーク、VOLT-SENTINELに紫の多重ハローを割り当てた。カード使用時は通常55ms、メガ枠105msの視覚ヒットストップを使い、ゲーム内タイマーは継続する。
- 1280×720ではSFX、音量、振動のコントロールが右上で独立して読め、盤面と危険予兆を妨げない。390×844では最初は戦闘ログと重なったため、敵情報パネル直下へ移動し、再キャプチャでログ・HUD・タッチ操作と非干渉であることを確認した。

## 命中方向ガイドメモ

- カード発動時に、自機から対象へ伸びる方向ビームと矢じりを追加した。横列・縦列・十字では対象の行列バー、敵領域では分岐ビームと外周、自己対象では上昇ハローを使い、危険マスの朱色予兆とは高さ・色・起点を分けている。
- Wave 2のデスクトップと390×844モバイル自動戦闘で、盤面上の命中点、カード起点の光線、敵の予兆を同時に確認した。方向ビームと矢じりは0.66〜0.78秒に延長し、短いヒットストップと併用して読み取りやすくした。

## 戦闘再構成メモ

- 初回カスタム画面では、カード列の下部に6×3の18マス範囲プレビューを追加した。自陣中央のPと、選択中カードの対象マスを分離した色で表示する。1280×720ではカード5枚、範囲盤、送信操作が同時に収まり、390×844では横スクロールのカード列と範囲盤が操作領域を奪わず配置できた。
- 通常攻撃は同一行の最も近い敵だけを対象とする正面直線判定へ変更した。敵攻撃は予兆時の固定対象マスを参照するため、弾道との接触ではなく着弾時に対象マスにいるかで被弾が決まる。
- デスクトップの停止デモでは、戦闘盤面を背面に残したままSFX・音量・振動を一箇所で設定できる停止メニューを確認した。390×844の戦闘画面では、停止ボタンを敵情報下に置き、大型化した十字移動キーとFIRE／CHG／CARDを並列に配置している。

## 範囲プレビュー・アニメーションメモ

- 18マス範囲プレビューの対象マスは、カードの対象形状に応じて順番に明滅し、白い走査線が通過する。正面・横列は左から右、縦列は上から下、十字は中央起点、敵領域は左から右に展開する。
- 1280×720と390×844の両方で、対象マスの走査、方向記号、カード列、送信ボタンが競合しないことを確認した。モーション低減設定では点滅を停止し、対象マスを静的に強調する。

## 操作応答性・自由選択メモ

- 英字コード表示と同コード・同名の選択条件を廃止し、カスタム画面ではカード系統名を表示する。選択説明も任意の組み合わせで最大5枚を送信できる内容に更新した。
- 5枚選択デモでは、先頭2枚が消費された後も残る3枚が送信キューに保持され、連続カード使用へ移行できることを確認した。停止時には17秒カスタムの残り時間も正しく凍結している。

## カード判定・選択フローメモ

- 初回タップは金色の説明フォーカスと18マス範囲プレビューを表示し、同じカードの2回目タップで選択する。選択済みカードには選択順の番号が表示され、異なる系統を任意の順に選択できることを確認した。
- カード使用時は対象形状から先に固定対象マスを決定し、そのマスにいる敵だけへ適用する。カード演出は固定マスの方向レール・走査・着弾リングに統一し、敵追尾に見える個別の弾道は通常攻撃だけに限定した。

## 戦闘整合性メモ

- 無敵付与は強襲転送と位相迷彩で5秒へ統一し、HUDに残り秒数を表示する。夢幻障壁は完全無敵ではなく、100未満の攻撃だけを防ぐ独立障壁として扱う。
- 近接斬、突進斬、月光剣は、敵領域の最前列3マスを対象に変更した。カード送信時には、固定範囲ガイドに加えてプレイヤー側から2本の切断アークを出す。
- 敵不在の通常・チャージ攻撃も、プレイヤーの行を敵領域最奥まで進む直線弾として描画する。スクリーンショット環境ではクエリ切替時に既存のReact状態が維持される場合があるため、範囲の最終確認は実際のカード選択操作でも行う。

## カード固有描写メモ

- カード送信イベントへ `cardId` を渡し、50枚すべてを描画レシピと個別分岐へ接続した。`pnpm audit:card-vfx` はカード定義50件、描画レシピ50件、描画分岐50件および不足なしを確認する。
- `?vfx=<cardId>` は開発確認専用の反復プレビューで、通常ゲームプレイには影響しない。ブラウザ確認では流星群の複数の落下軌道・着弾リングが盤面上に現れることを確認した。
- 近接斬など自陣側で発生する短い描写は、カスタム画面のカードオーバーレイに隠れやすい。最終画面確認では `?vfx=<cardId>&demo` で戦闘画面へ遷移させ、オーバーレイなしの盤面で確認する。
- `?vfx=slash&demo` の戦闘盤面では、近接斬の白金・朱色の二重切断アークと対象列の固定ガイドが重なって表示されることを確認した。カード送信中の短い描写は、カード選択オーバーレイに遷移したフレームでは隠れるため、戦闘中フレームを基準に判定する。
- `?vfx=magguard&demo` の戦闘盤面では、自機を囲む電磁ノードと格子線がプレイヤー移動後の現在マスへ追従して生成されることを確認した。実際の送信時は一度だけ発生し、開発プレビューだけが反復する。

## カード音響・命中反応メモ

- 全50枚にカードID単位の発振波形・音列・間隔・ノイズ成分を持つ合成音レシピを追加した。既存のSFXオンオフと音量スライダーはそのままレシピ出力を制御する。
- 命中イベントに敵ID、カードID、威力、状態、チャージ、カウンター情報を加えた。敵側はBULWARK-3の後退、SCANNER-8の走査リング、RAZOR-6の跳ね、MORTAR-NODEの反動、VOLT-SENTINELの電気的横揺れで反応を分ける。
- 強力カード用の画面フラッシュおよび強度設定は、ユーザーの指示により実装対象から外した。`?vfx=overdrive&react=bulwark&demo` の戦闘画面で、フラッシュなしに大型のカード固有描写とBULWARK-3向けの命中リアクションが重なることを確認した。

## プレイヤー被弾・ガード反応メモ

- 被弾、障壁防御、位相回避、カウンター、位置回避を専用イベントとして区別し、ゲームのダメージ・無敵・障壁の既存ルールは変えずにシーン側の短時間演出へ接続した。
- `?playerreaction=damage&demo` では、朱色の足元衝撃リング、短い後退・縮み、スプライト点滅がプレイヤー周辺に現れ、反応の終了後に通常姿勢へ復帰することを確認した。ガード系は白金の防壁板、位相の水色残像、カウンターの前方返し線として被弾と視覚的に区別する。
- `?playerreaction=barrier&demo` では、プレイヤー前方の白金防壁板とティールの二重リングを確認した。被弾の朱色リングとは色・方向・姿勢が異なる。確認後の追加閲覧ではブラウザ実行環境が利用できなくなったため、最終検証は型・ビルド・自動監査も併用する。

## ピンチ状態の警告メモ

- HP比率が30%以下では注意、15%以下では緊急状態へ遷移する。危険域を脱した際はReactのスナップショット更新によりフレーム・HUD表示を即座に通常へ戻す。
- `?panic&demo` の戦闘画面で、HP 28およびHP 4の緊急状態に赤い外周フレーム、赤いHP数値、`CRITICAL // EVASIVE ACTION` 表示が現れることを確認した。フレームはポインター入力を受けない最前面装飾であり、停止・ROUTE C・盤面操作を覆わない。
- モバイル専用の自動キャプチャは実行環境で失敗したため、既存のモバイル用HUDレイアウトと、フレームの固定・ポインター非捕捉スタイルを用いて干渉しない構成を維持している。
