# Game Plan: Grid Signal Arena

## 参照した戦闘形式

**開始時と戦闘20秒ごとの5枚カード提示、提示された1〜5枚の自由選択、3×3の自陣での上下左右移動、3発連射後2秒休止の通常攻撃、溜め射撃、選択順のカード使用、攻撃予兆に対するカウンター、勝敗リザルト**を、固有名詞・キャラクター・画面意匠を使わないオリジナルのルールに再構成する。

## リスクタスク

### 1. リアルタイム・グリッド戦闘

- **なぜ隔離するか：** キーボード入力、3×3マスの制約、敵AIの予兆・発射・ヒット判定、画面上のエフェクトが同一の更新ループで動くため、個別の不具合が連鎖しやすい。
- **方針：** 各ユニットをグリッド座標で保持し、Babylonの位置補間は座標の視覚化のみに使う。敵は明示的な `idle → windup → fire → recover` 状態で動かし、スキル命中が `windup` 中ならカウンター判定を優先する。
- **検証：** PCは矢印キーで、タッチ端末は画面キーで全方向へ一マスずつ移動でき、領域境界を越えない。敵の予兆、攻撃演出、床の対象マスが同じ座標を示し、予兆中のカード命中と通常被弾を混同しない。

### 2. カスタム・コンソールと手札の状態遷移

- **なぜ隔離するか：** 初回選択、20秒ごとの再選択、選択済みカードの消費、結果画面という複数のモードを誤りなく切り替える必要がある。
- **方針：** `custom / battle / result` を明示的なゲームモードにし、カード選択画面は入力を専有する。公開カタログから未提示カードをランダムに5枚示し、種類や接続コードに関係なく1〜5枚を選択できる。確認後は決定済みカードを順番に消費し、20秒経過時は自動で選択画面へ戻す。
- **検証：** 最初と20秒後に5枚の日本語名カードが示され、同一プレイ中にカードIDが重複せず、5枚すべてを選択して、確認後に選択順で対象形状・状態効果・カードカウンターが正しく反映される。

## メインビルド

Babylon.js上に奥行きをもつ6×3タイルのアリーナを構成し、プレイヤーのティール領域と敵のオーカー領域を明確に分ける。オリジナルのシグナル・パイロットと二種のドローンを配置し、通常射撃、溜め射撃、カードスキル、カウンター、同期倍率、敵の複数攻撃、勝敗・評価リザルトを実装する。HTMLオーバーレイは補助的な端末HUDとして、耐久、カード待ち時間、カードキュー、操作ガイドを表示する。

- **アセット：**
  - `grid-signal-arena-reference` — 16:9の画面構成・色・盤面密度の参照。
  - `grid-signal-arena-pilot` — プレイヤー用テクスチャ。盤面内で約1.2m高。
  - `grid-signal-arena-shield-drone` — 近距離ドローン用テクスチャ。盤面内で約1.1m高。
  - `grid-signal-arena-sensor-orb` — 浮遊ドローン用テクスチャ。盤面内で約0.9m高。
  - `grid-signal-arena-mark` — 起動画面・端末ヘッダー・ファビコン用のロゴ。
- **検証：**
  - キーボード・クリック・タッチボタンの入力と表示が一致する。
  - UIに重なりや文字切れがなく、モバイルでも操作手段が失われない。
  - プロジェクタイル、チャージ、命中、カウンター、敵の予兆が見た目で判別できる。
  - 戦闘の開始から結果までが一回のプレイで完結する。
  - 画像の欠落、目立つプレースホルダー、ブラウザコンソールエラーがない。
  - 生成した参照画の配色、視点、UI密度、盤面優先の構図と整合する。
- `?demo` で決定論的な自動戦闘を実行でき、スクリーンショットに実ゲーム状態を表示する。
- PC版だけにキー設定を表示し、通常攻撃・チャージ・カード使用へ任意のキーを割り当てられる。矢印キーは移動専用として保護する。
