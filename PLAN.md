# Game Plan: Grid Signal Arena

## 参照した戦闘形式

指定マニュアルのうち、**開始時のランダムな手札選択、相性条件を満たす複数選択、3×3の自陣での上下左右移動、無制限の通常射撃、溜め射撃、送信済みスキルの順次使用、ゲージ満タン後の再選択、攻撃予兆に対するカウンター、勝敗リザルト**を、固有名詞・キャラクター・画面意匠を使わないオリジナルのルールに再構成する。

## リスクタスク

### 1. リアルタイム・グリッド戦闘

- **なぜ隔離するか：** キーボード入力、3×3マスの制約、敵AIの予兆・発射・ヒット判定、画面上のエフェクトが同一の更新ループで動くため、個別の不具合が連鎖しやすい。
- **方針：** 各ユニットをグリッド座標で保持し、Babylonの位置補間は座標の視覚化のみに使う。敵は明示的な `idle → windup → fire → recover` 状態で動かし、スキル命中が `windup` 中ならカウンター判定を優先する。
- **検証：** 矢印/WASDで全方向へ一マスずつ移動でき、領域境界を越えない。敵の予兆中にスキルを当てると停止・同期状態・次のスキル倍率が正しく発生し、通常の被弾と混同しない。

### 2. カスタム・コンソールと手札の状態遷移

- **なぜ隔離するか：** 初期選択、同一シグナルまたは同一レーンの組み合わせ制限、選択済みスキルの消費、ゲージ満了後の再選択、結果画面という複数のモードを誤りなく切り替える必要がある。
- **方針：** `custom / battle / result` を明示的なゲームモードにし、カスタム画面は入力を専有する。カードフォルダから5枚を提示し、**同名または同じコード**のカードのみ最大5枚まで追加選択できる。メガ枠は低頻度で提示し、選択済みのメガ枠は同時に1枚までとする。確認後は決定済みカードを順番に消費する。
- **検証：** 最初に5枚の日本語名カードとコードが示され、同名または同コードの二枚目だけを追加できる。通常枠とメガ枠が区別でき、確認後は対象形状・状態効果・カードカウンター・フルシンクロが正しく反映される。

## メインビルド

Babylon.js上に奥行きをもつ6×3タイルのアリーナを構成し、プレイヤーのティール領域と敵のオーカー領域を明確に分ける。オリジナルのシグナル・パイロットと二種のドローンを配置し、通常射撃、溜め射撃、カードスキル、カウンター、同期倍率、敵の複数攻撃、勝敗・評価リザルトを実装する。HTMLオーバーレイは補助的な端末HUDとして、HP、ゲージ、状態、スキルキュー、操作ガイドを表示する。

- **アセット：**
  - `grid-signal-arena-reference` — 16:9の画面構成・色・盤面密度の参照。
  - `grid-signal-arena-pilot` — プレイヤー用テクスチャ。盤面内で約1.2m高。
  - `grid-signal-arena-shield-drone` — 近距離ドローン用テクスチャ。盤面内で約1.1m高。
  - `grid-signal-arena-sensor-orb` — 浮遊ドローン用テクスチャ。盤面内で約0.9m高。
  - `grid-signal-arena-mark` — 起動画面・端末ヘッダー・ファビコン用のロゴ。
- **検証：**
  - キーボード・クリック・タッチボタンの入力と表示が一致する。
  - UIに重なりや文字切れがなく、モバイルでも操作手段が失われない。
  - プロジェクタイル、チャージ、命中、カウンター、敵の予兆が見た目で判別できる。
  - 戦闘の開始から結果までが一回のプレイで完結する。
  - 画像の欠落、目立つプレースホルダー、ブラウザコンソールエラーがない。
  - 生成した参照画の配色、視点、UI密度、盤面優先の構図と整合する。
  - `?demo` で決定論的な自動戦闘を実行でき、スクリーンショットに実ゲーム状態を表示する。
