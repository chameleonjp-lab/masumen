/** Signal Relay Tactical app shell: intentionally no page chrome beyond the full-screen game terminal. */
import GameCanvas from "@/components/GameCanvas";

export default function App() {
  return <GameCanvas />;
}

