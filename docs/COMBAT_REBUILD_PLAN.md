# 戦闘再構築・実装計画書

対象リポジトリ: `chameleonjp-lab/masumen`  
基準ブランチ: `main`  
計画書の位置づけ: 戦闘実装・検査・仕様判断の基準文書  
作成方針: 物語、人物、固有名称、画像、音楽、画面構成を模倣せず、リアルタイムの判断と駆け引きを独自のゲームとして実装する。

> **2026-09-13 現行仕様の上書き**：この日付以前のPR記録にあるフォルダ編集、練習モード、精神状態、ゲージ満タン時の手動選択、英語の敵名は履歴であり、現行ゲーム画面・通常戦闘には適用しない。開始は「信号を開始」→名前入力→カード選択とし、公開カタログから未提示の5枚を提示する。戦闘中は20秒ごとに自動でカード選択へ戻り、1〜5枚を選んで再開する。通常攻撃は3発連射後2秒休止、PC版は矢印キー移動と設定可能な攻撃キー、タッチ端末は三角配置の大型操作ボタンを使う。

## 1. 最終目標

masumenを、6列×3行の盤面で攻撃を避けるだけのゲームから、次の判断が連続するリアルタイム戦闘へ作り直す。

1. ホーム画面から名前を入力して開始する。
2. 公開カタログから未提示の5枚をランダムに提示し、1〜5枚を選ぶ。
3. 敵の位置、防御、攻撃準備、パネル、障害物を見てカードを使う。
4. 弾道、設置物、近接、地形、属性相性を使い分ける。
5. 敵の攻撃直前にカードを当て、カウンターを取る。
6. 戦闘20秒ごとに次の5枚を選んで戦闘へ戻る。
7. 時間、被弾、カウンター、同時撃破をスコアへ反映する。

ソウルユニゾン、探索、会話、対人対戦、恒久強化、オンラインランキング、課金、広告、アカウント、開放制限は実装しない。カードカタログ50枚は最初から利用できる。

## 2. 採用する戦闘要素

- 盤面は6列×3行。開始時は左3列がプレイヤー陣地、右3列が敵陣地。
- 通常射撃、距離別発射間隔、チャージ射撃、カード攻撃を同じ戦闘時計で処理する。
- カードは全カタログから抽選し、同一プレイ中にカードIDを重複させない。表示された5枚は名前・接続コードに関係なく1〜5枚を選択できる。
- 攻撃は直進弾、貫通、多段、幅広波、投擲、近接、踏み込み、追尾、反射、周回、設置、罠、地形変化、自己効果を個別に扱う。
- パネルは通常、亀裂、穴、草、氷、溶岩、毒、聖域を持つ。属性は無、炎、水、電気、木とする。
- 敵の攻撃には待機、移動、準備、カウンター受付、発動、回復の段階を持たせる。
- 精神状態に関する表示・概念・効果は実装しない。カード選択、カウンター、地形、攻撃予兆だけを戦闘判断として扱う。
- 敵の名前・行動説明・カード説明は端的で丁寧な日本語に統一する。

## 3. 固定する初期値

この表は初期調整値であり、後から`balance.ts`へ集約する。判定方式と数値調整を混ぜない。

| 項目                         |         初期値 |
| ---------------------------- | -------------: |
| 内部更新                     |        60回/秒 |
| 長い中断として破棄する実時間 |    250ミリ秒超 |
| 通常射撃威力                 |             12 |
| チャージ射撃威力             |             42 |
| チャージ完了                 |      850ミリ秒 |
| 次のカード選択               |           20秒 |
| 通常攻撃                     | 3発後に2秒休止 |
| 通常被弾後の操作不能         |      180ミリ秒 |
| 通常被弾後の短い被弾無効     |      350ミリ秒 |
| Wave終了時の回復             | 最大体力の15％ |
| 通常モードのWave数           |              4 |

画面を閉じた時間、Safariから戻るまでの長い経過時間、一時停止中、カード選択中、命中停止中は、敵、弾、パネル、ゲージを進めない。描画回数が30・60・120回/秒でも戦闘結果を変えない。

## 4. 実装順

順番は変更しない。個別カードの例外処理を基盤より先に増やさない。

|  PR | 実装範囲                                                                                         | 完了条件                                                                   |
| --: | ------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------- |
|   1 | 計画書固定、現行動作の保護、カード・敵の基準監査、型検査・テスト・ビルド                         | mainの現行カタログ50枚、5系統の敵、4Waveが検査可能                         |
|   2 | 固定時間更新、ゲーム時刻、実時間との分離、一時停止、中断復帰、再現可能乱数、状態初期化、命中停止 | 30・60・120描画で同じ結果、復帰後の瞬間移動なし、10回再戦で状態持越しなし  |
|   3 | 18マス、8パネル、陣地、陣地拡張、設置物、氷の滑り、穴の進入禁止                                  | パネル遷移、陣地復帰、設置物重複を自動検査                                 |
|   4 | 弾道・衝突の共通基盤、通常射撃、距離別連射、1ボタン式チャージ、被弾無効                          | 対象なしでも弾が進み、多段・障害物・近接・踏み込みが別処理                 |
|   5 | 全カタログからのランダム5枚、提示済みID管理、1〜5枚選択                         | 5枚提示、同一プレイ中のID重複なし、種類・コードによる拒否なし              |
|   6 | 20秒自動再選択、カウンター、カード効果                                     | 選択中に戦闘時計が止まり、確定後に再開する |
|   7 | 射撃・範囲・属性・近接の21枚                                                                     | 21枚の実処理、範囲表示、属性とパネル相互作用が一致                         |
|   8 | 設置・地形・防御・反撃・回復・補助の25枚                                                         | 設置物、地形変化、障壁、オーラ、位相化、身代わり、ゲージ効果が別処理       |
|   9 | 上位・切札、連結技8種、全カード監査                                                              | 50枚すべてに処理・描写・音・説明があり、順番違いは通常カードとして扱う     |
|  10 | 現在の敵5種類の再構築                                                                            | 各敵2行動以上、個別カウンター、攻撃中の隙、回避可能な編成                  |
|  11 | 新敵7種類、ボス4種類、段階変化、編成、安全マス検査                                               | 通常敵12種、ボス4種、同じ攻撃だけで完封不可                                |
|  12 | Wave進行、Wave回復、スコア、ランク、結果、ローカル記録                                | 通常・敗北・相打ち・再戦が完結する                     |
|  13 | 日本語化、縦横画面、iPhone SE相当、入力・セーフエリア対策                                        | 横スクロール、入力ずれ、長押し残留、二本指二重発射がない                   |
|  14 | 描写・音・負荷・ブラウザ検査・最終敵対的検証                                                     | 10回連続プレイ、10回再戦、コンソール、速度、画像欠落を検査                 |
|  15 | ホーム・名前入力、20秒再選択、三角操作、敵座標同期、PCキー設定、日本語化                         | 現行仕様の自動テスト・監査・ビルドが通り、実機受入項目を明示する           |

## 2026-08-29 開始前選択の再設計

原作調査と現行コードの照合により、「開始前に恒常スキルを50個から1つ選ぶ」案は採用しない。開始前は30枚フォルダの準備、戦闘中はランダム提示された5枚から選ぶアクティブカード、という二層に整理する。50枚の効果・役割・対抗対象は docs/START_SELECTION_REDESIGN.md に集約する。

## 5. 今回のPRで固定した基盤

今回の最初の作業では、既存のカード効果や敵の種類を一度に置き換えない。`GameWorld`が描画フレームの実時間へ直接依存しないようにし、次を先に固定する。

- `FixedStepClock`が60Hzの更新を担当する。
- 250ミリ秒を超えるフレーム間隔は、追いつき処理をせず破棄する。
- ゲーム内部の時刻は`GameWorld`の経過時間だけを使う。
- 一時停止、カード選択、命中停止中は内部の敵・弾・ゲージを進めない。
- 再戦時に時計、命中停止、チャージ、敵、弾、スコア、経過時間を初期化する。
- ページ非表示、フォーカス喪失、ページ離脱、`pointercancel`でチャージを解除する。
- 乱数は`Random`へ集約し、同じ種から同じ列を再現できるようにする。

## 6. PR3で固定した盤面基盤

- `PanelSystem`が6列×3行、18枚のパネルを所有し、初期所有権を左右3列へ分ける。
- 通常、亀裂、穴、草、氷、溶岩、毒、聖域を個別の地形状態として保持する。
- 亀裂パネルは占有者が離れた時に8秒間の穴となり、その後通常へ戻る。
- 氷への進入は入力方向へ滑り、穴、障害物、占有者、陣地境界で停止する。
- 敵前列1列の一時的な自陣化と、期限切れ時の安全な自陣帰還を共通処理にする。
- `ObjectSystem`が爆弾、地雷、砲台、キューブ、杭、フィールド装置を保持し、陣営ごとに最大6個、寿命、HP、衝突種別、発動条件を管理する。
- 盤面スナップショットを描画側へ渡し、地形は色だけでなく亀裂、穴、草、氷、溶岩、毒、聖域の形状でも判別できるようにする。
- Wave終了と再戦ではパネル、一時地形、所有権、設置物、占有者を初期化する。

## 7. データ構成の目標

最終的には`GameWorld.ts`へ処理を追加し続けず、次の責務へ分ける。

```text
client/src/game/
├── core/       BattleEngine, BattleState, FixedStepClock, Random, EventQueue
├── systems/    Movement, Attack, Projectile, Object, Panel, Status, Counter,
│               Custom, Emotion, Enemy, Score
├── data/       cards, cardCodes, chainTechniques, overloadCards, enemies,
│               encounters, balance
└── render/     ArenaRenderer, EffectRenderer, CardVfxRenderer, AudioManager
```

React側はHUD、カード選択、フォルダ編集、操作、一時停止、結果、練習を分ける。Babylon.js、React、Viteは維持する。

## 7.1 PR4で固定した攻撃基盤

- `ProjectileSystem`が攻撃物を盤面上の状態として保持し、固定更新ごとに移動させる。
- 対象がいなくても盤面端まで進み、通常弾は最初の敵・障害物で停止し、貫通弾は敵を通過する。
- 多段は複数の攻撃物として生成し、幅広波は同じ列の3行を衝突対象にする。
- 投擲は指定マスへ着弾し、追尾は毎回の移動前に対象方向を更新する。
- 反射は盤面端で方向を反転し、周回は外周で進行方向を曲げる。
- `AttackSystem`が近接の同一行判定、十字判定、踏み込み先と安全な帰還位置、攻撃時間の境界を共通化する。
- 通常射撃は同じ行の敵までの距離で210・260・320ミリ秒の再発射間隔を選び、チャージは850ミリ秒で完了する。
- 敵の攻撃は発射予定だけを保存せず、攻撃物を生成してプレイヤー、障壁、設置物との衝突を解決する。
- 設置物は時間、接触、一定間隔の発動を持ち、爆弾、地雷、砲台、杭、毒霧の最低限の共通発動経路を持つ。
- 通常被弾時は180ミリ秒の操作不能と350ミリ秒の被弾無効を適用する。同時解決では敵側の攻撃を先に解決し、相打ちを敗北扱いにする。

カード個別の正確な威力、属性、連結技、フォルダ抽選はPR5〜PR9でデータ定義へ移す。PR4では判定方式を先に固定し、個別カードの例外を増やさない。

## 7.2 旧PR5記録（フォルダ基盤）

端末保存・フォルダ編集・接続コード制限は過去の検討と実装履歴として残す。現行のカード選択では参照せず、`BattleDeck`が公開カタログから未提示IDを抽選する。

## 7.3 旧PR6記録（ゲージ・精神状態・過負荷）

ゲージ満了、精神状態、過負荷カード、練習段階は現行の通常戦闘仕様から廃止した。互換テストと保存データの読み込みを壊さないため内部型を残しているが、通常戦闘の効果・画面・選択肢には影響しない。現行の再選択条件は戦闘時間20秒である。

## 7.4 2026-09-02 ゲームプレイレビューで確定したルール（現行）

- ヒットストップは演出だけでなく戦闘時計を止める。停止中は敵、弾、パネル、ゲージ、通常のプレイヤー入力を進めず、カードの再送も受け付けない。進行中の超過駆動入力だけは入力シーケンスを失わないため受け付ける。
- カスタム復帰はWaveの初回確認時だけ敵の開幕攻撃間隔を設定する。戦闘中にカスタムを開閉しても、敵ごとの`nextAttackAt`、`windupUntil`、`recoverUntil`、予兆対象を再初期化しない。
- カード選択中は固定更新を止め、戦闘時間20秒で自動的に次の5枚を提示する。確定後は選択順のカード列で戦闘を再開する。
- 敵の弾は発動時に予兆で保存した対象マス集合を持ち、弾の現在位置がその集合内でプレイヤーと重なった時だけ命中する。弾の描画経路全体を無条件の命中範囲にはしない。
- 行を対象にする直進弾は、敵の現在行ではなく予兆で固定した対象行から発射し、警告表示と弾道の行を一致させる。
- 通常の多段近接は一つの被弾イベントとして合算し、通常の350ミリ秒被弾無効を各アクションへ適用する。
- 通常プレイのラン種は開始ごとに変える。`?demo`または`?seed=<整数>`では再現可能な種を使い、テストとデモを固定できるようにする。
- カード選択への遷移はゲージではなく戦闘時間20秒で決める。選択開始時にチャージとヒットストップだけを解除し、敵の攻撃予定・予兆・麻痺時間は保持する。
- カスタム画面のカードは1回のタップで選択し、選択中のカードをもう一度タップすると解除する。説明と範囲表示は選択前から確認でき、2回タップを前提にしない。
- 視界遮断中も盤面の危険予兆は残す。敵HUDの予兆テキストと対象一覧だけを隠し、プレイヤーHUDには視界遮断の残り時間を表示する。
- 地形ダメージは、直前の通常被弾から残っている350ミリ秒の被弾無効を共有する。ただし、地形ダメージ自身は新しい被弾無効時間を開始しない。
- Wave遷移では一時バフ、未使用キュー、設置物、予兆、弾を破棄し、最大HPの`wave.recoveryRatio`だけ回復する。

## 7.5 2026-09-03 公開起動失敗の復旧

- WebGLが利用できない環境でも同期的な描画エンジン生成例外を画面全体へ伝播させず、再読み込み案内を表示する。ゲーム戦闘を代替描画へ変更せず、利用できない環境では開始不能の理由を明示する。

## 8. 検査方針

各PRで次を実行できる状態を作る。

```text
pnpm audit:card-vfx
pnpm audit:combat-data
pnpm audit:encounters
pnpm audit:board-data
pnpm audit:attack-data
pnpm audit:folder-data
pnpm test
pnpm check
pnpm build
```

後続PRでブラウザ検査を追加し、カード、敵、パネル、乱数、同時決着、再戦初期化を個別に検査する。説明・範囲・描写・音は同じカード定義から参照し、手作業の二重管理を増やさない。

## 9. 著作物との区別

現在の「Signal Relay Tactical」、ティールとオーカーの陣地、独自の敵名、独自カード名を維持する。参考作品の人物、名称、画像、画面写真、音声、音楽、台詞、物語、ロゴ、カード枠、対戦方式は含めない。参考作品名は開発資料内の比較対象に限る。

## 10. 完成条件

全カタログからの未提示5枚、20秒自動再選択、50枚の固有判定、8パネル、4属性、設置物、敵12種、ボス4種、4ウェーブ、スコア、ランク、丁寧な日本語表示、縦横画面、PCキー設定、端末差のない固定更新、中断・回転・再戦、型検査・単体検査・監査・ビルド・ブラウザ検査をすべて満たした時点で完了とする。フォルダ編集、練習モード、精神状態、英語の敵名は完了条件に含めない。

未検証の項目は完了扱いにせず、PR本文と最終仕様書へ残す。


## PR6後のPR7実装記録

PR7では、射撃・範囲・属性・近接の21枚を、カード定義だけでなく実際の攻撃物と対象範囲へ接続する。

- 連射弾、三連砲は固定間隔の独立弾として生成する。
- 貫通槍は貫通弾、広角弾と凍結波は全行を覆う波、十字砲は直進40と着弾地点の十字追加20として処理する。
- 縦列砲と雷列は、その時点で最も近い敵の列を使う。扇状弾は3方向、電撃鎖は追尾と近接連鎖を使う。
- 氷壁弾は40ダメージの投擲後にHP70の障害物を残し、凍結波は空きパネルを氷へ変える。
- 炎・水・電気・木の属性と、草・溶岩・氷のパネル反応を共通処理で解決する。
- 近接斬、横薙ぎ、突進斬、格子断、月光剣は、攻撃範囲・踏み込み・帰還・二段判定を別々に扱う。

範囲表示は固定の自陣中央ではなく、現在位置と現在の敵位置から共通のカード範囲計算を参照する。

## PR7後のPR8実装記録

PR8では、設置、地形・陣地、防御・反撃、回復・補助の25枚を、共有状態へ接続する。

- 時限地雷、監視地雷、砲台ポッド、拘束杭、毒霧装置を、寿命・HP・発動条件・設置上限を持つ盤面オブジェクトとして扱う。監視地雷はhidden状態を持ち、敵が踏んだ時だけ表示・発動する。
- 破砕柱は投擲攻撃とパネルの亀裂化を組み合わせ、遮断キューブは固体障害物として弾道と移動を遮る。
- 聖域セル、亀裂線、区画拡張、逆位相穴は、カード使用時にPanelSystemへ地形・所有権を反映する。強襲転送は位相化と次の剣攻撃の補正を状態として保持する。
- 引力場は一定間隔で敵を中心へ移動させ、乱流壁は敵の障壁を半減したうえで敵と押せる設置物を後退させる。毒霧装置は毒パネルと周期ダメージを共有する。
- プリズムガード、位相迷彩、返し手裏剣、身代わり膜、電磁防壁、予知反撃を、障壁・位相・反撃姿勢・安全マス移動・放電へ分離した。応急修復は準備中の被弾で失敗し、完全同期や通常の被弾処理と競合しない。
- 整流回復、速攻同期、出力印、再送回路は、HP、カスタムゲージ、次の攻撃命中の状態を直接更新する。出力印は最大4回まで1命中30を加算する。
- FieldObjectへ効果識別子、ダメージ元、隠し状態、押し出し可否を追加し、表示・攻撃・移動・監査が同じ設置物情報を参照する。
- 敵陣対象の範囲表示は固定位置ではなく、盤面の敵陣3列を返す。カード定義、実処理、範囲表示、監査対象をPR7対象カードと同じ形式で検査する。

自動検査では、PR7・PR8対象46枚の定義、処理フック、デッキ登録、威力・範囲メタデータ、設置物メタデータ、聖域・防御・修復処理を確認する。スマートフォン実機、特にiPhone SE相当・iPhone 11 Pro・iPhone 17 Proの縦横操作は、この作業環境では未検証として残す。

## PR9実装記録

PR9では、既存の上位カード「月光剣」を含む上位4枚・切札1枚の構成を完成させる。新規追加は上位3枚と切札1枚で、カード総数50枚を定義・監査する。

- 流星群は敵陣へ8発の投擲弾を生成し、着弾地点を亀裂化する。夢幻障壁は8秒間、100未満の攻撃だけを無効化し、100以上の攻撃で消滅する。聖域展開は現在の全自陣を10秒間聖域化し、HP50を回復する。
- 超過駆動は最も近い敵へ接近し、バスター入力を3回の受付へ使う。各成功で70ダメージを与え、3回目に対象周囲を亀裂化する。
- 連結技は選択順と選択済み接続コードを選択済みカード全体で検査し、完全一致した時だけ個別カードを消費して連結カードへ変換する。不一致時は通常カード列として処理する。
- 連鎖掃射、三重月断、火界連鎖、樹牢陣、地盤崩壊、雷磁包囲、多層防衛、完全修復の8種類を、攻撃物、遅延効果、設置物、地形、状態、回復の共有処理へ接続した。
- 連結カードの演出・音は、消費された構成カードの既存定義を順番に参照する。連結技の使用IDは戦闘スナップショットへ記録し、監査スクリプトで8件の定義・変換・個別処理を確認する。
- `audit:card-combat`を50枚対応へ拡張し、`audit:chain-data`を追加して、カード定義・処理・演出・音・連結変換をCIで検査する。
- 端末実機でのiPhone SE相当、iPhone 11 Pro、iPhone 17 Proの縦横操作、および連結技の視認性・入力感覚は、この作業環境では未検証として残す。



## PR10実装記録

PR10では、既存の5敵を、敵ごとの行動定義・移動方式・防御方式・攻撃中の隙へ分離して再構築した。

- BULWARK-3は待機中の正面ガードを持ち、横一列砲撃と近距離盾打ちを交互に使う。破砕カードまたはカウンター受付中はガードを破れる。通常射撃は最低限の反撃手段として通る。
- SCANNER-8は飛行方式で穴を無視し、縦列走査と追尾信号弾を使う。追尾信号弾が命中すると短時間の目隠しを適用し、次の攻撃予告を短くする。
- RAZOR-6はプレイヤーの行へ踏み込み、踏み込み斬りと横薙ぎを使い分ける。近接攻撃は同じ行または十字範囲だけを判定し、攻撃後に敵陣内へ戻る。
- MORTAR-NODEは移動せず、山なり砲弾・三点砲撃・破壊可能な敵地雷を使う。敵地雷は盤面上の実体として残り、射撃で破壊できる。
- VOLT-SENTINELは飛行・電気属性で、交互マス電撃と連鎖電撃を使う。連鎖電撃が命中すると移動と攻撃を短時間停止する。
- すべての攻撃に100〜180ミリ秒の個別カウンター受付時間、準備・受付・発動・回復の段階を持たせた。攻撃予告、発射、被弾判定は固定時間更新で進む。
- 敵データ監査と行動・防御・攻撃隙の単体検査を追加した。既存の4Wave編成、敵ID、再戦時の状態初期化は維持する。

iPhone SE相当、iPhone 11 Pro、iPhone 17 Proの縦横実機操作、実機上の敵攻撃予告の見え方、目隠し・麻痺中の入力感覚はこの作業環境では未検証として残す。


## PR11実装記録

PR11では、新しい通常敵7種とボス4種を、既存の固定時間更新・盤面・攻撃物・設置物の共通処理へ接続した。

- WAVE-RUNNER、BOOMER-ARC、HOPPER-BOMB、GAIA-HAMMER、WEATHER-CORE、SUPPORT-RELAY、MIRROR-NODEを追加した。三行水波、外周往復弾、跳躍着地爆発、爆弾投下、装甲槌撃、地震、状況別属性攻撃、味方修復・障壁、射撃反射・模倣射撃を個別の行動として定義した。
- BASTION PRIME、PRISM HUNTER、CLIMATE ENGINE、CORE ARBITERを追加した。体力比率による段階表と段階別行動を持ち、カウンター成功後にCORE ARBITERの行動順を変える。
- WEATHER-COREとCLIMATE ENGINEは、プレイヤーが草・氷にいる場合の状況を見て属性行動を選び、行動中の弱点属性をカードダメージへ反映する。
- ボスの直近2体を避ける抽選履歴を端末内へ保存する。再戦では戦闘状態だけを初期化し、ボス履歴は次の抽選のために保持する。
- Wave1〜3は9種類の検査済み編成テンプレートから選ぶ。Wave4はボス1体を基本とし、特定ボスでは支援敵を最大1体追加する。開始時の安全マス、敵の重複配置、敵陣外配置、ボス複数配置を監査する。
- 敵の時限爆弾、陣地奪取時の前列所有権変更と安全経路確保、周回弾の外周折り返しを共通処理へ追加した。
- PR11_NORMAL_ENEMY_IDS、BOSS_ENEMY_IDS、encounters.ts、定義監査、敵定義テスト、編成安全性テストを追加した。CIで監査、単体テスト86件、型検査、ビルドを実行する。

CIでの型検査、単体テスト、データ監査は成功した。iPhone 17 Pro、iPhone 11 Pro、iPadの実機操作、画面回転、Safari中断復帰、長時間連続プレイ、実機上の攻撃予告の見え方は、この作業環境では未検証として残す。


## PR12実装記録

PR12では、4Waveを開始から結果まで評価できるように、Wave終了処理、スコア、結果表示、練習モード、ローカル記録を接続した。

- Waveごとに基準時間、Wave突破点、無被弾点、時間点を計算し、敵撃破、カウンター、同時撃破、被ダメージ、過負荷の点数を共通スコア内訳へ集約した。スコアは0未満にならない。
- 同時撃破は固定時間内の追加撃破だけを数え、Wave終了時の回復は現在の最大体力の15％を基準に、侵食による回復量低下も反映する。
- 結果は勝利、敗北、相打ちを分け、相打ちはランクRとして扱う。S〜Dは初期スコア基準率で算出し、最終スコア、時間、到達Wave、被ダメージ、カウンター、同時撃破、使用カード、連結技、過負荷、自己ベスト差を表示する。
- ローカル最高記録はSaveDataV1のrecordsへ保存し、既存の記録キーからも読み戻せるようにした。練習モードの進行や終了は通常モードのスコア・最高記録へ反映しない。
- 練習モードは移動・通常射撃、弾道、接続コード、予告と回避、カウンターと完全同期、パネルと設置物、精神状態と過負荷の7段階を持つ。

iPhone 17 Pro、iPhone 11 Pro、iPadの実機操作、縦横回転、Safari中断復帰、長時間連続プレイ、結果画面の実機上の視認性は、この作業環境では未検証として残す。


## PR13実装記録

PR13では、日本語表示、縦横画面、iPhone SE相当の狭い幅、入力とセーフエリアを整備した。

- HUD、結果、練習、カード選択、フォルダ編集の表示文言を日本語へ揃え、敵行動の未翻訳フォールバックも日本語化した。
- viewport-fit=cover、dvh/svh、safe-area-inset、orientationchange、visualViewport、ResizeObserverを使い、画面回転・ブラウザUI変化後も描画サイズと操作面を更新する。
- 縦画面のカード選択は2列、横画面は5列とし、カード選択・範囲表示・フォルダ編集の横スクロールを禁止した。カード名と説明は狭い幅で折り返す。
- 移動、通常攻撃、チャージ、カード使用を同じPointer入力状態で管理し、二本目の指を無視する。通常攻撃の短い二重入力を抑止し、pointerup、pointercancel、pointer capture喪失、blur、visibilitychange、pagehideでチャージを解除する。
- タッチガードの単体テストを追加し、同時タッチ、短い二重発射、チャージ終了の対応関係を固定した。

iPhone 17 Pro、iPhone 11 Pro、iPhone SE相当、iPadでの実機操作、回転、Safari中断復帰、長時間連続プレイは、この作業環境では未検証として残す。


## PR14実装記録

PR14では、最終検証として連続プレイ、再戦、カード描写・音、シーンのリサイズ監視を自動検査した。

- 4Waveの完走から結果、再戦後の初期化までを10回連続で実行し、敵、設置物、弾、選択状態、体力、時刻の持ち越しがないことを確認する。
- 画像9件、カード描写50件、カード音50件の契約監査をCIへ追加した。実機の画像配信、コンソール、FPSはこの環境では未検証として残す。
- Babylon.jsのリサイズ監視をシーン破棄時に解除し、画面を作り直した時に監視が蓄積しないようにした。
- Actions run 58で、全監査、単体テスト、型検査、ビルドが成功した。

## PR15実装記録

PR15では、Manusの実行環境や外部保存先がなくても公開先だけでゲームを表示できるように、メディアと入口をローカル化する。

- 背景、プレイヤー2種、敵5種、ロゴをリポジトリ内のSVGとして同梱する。
- `ASSET_URLS`、ファビコン、Viteのビルド基準を相対パスへそろえ、ドメイン直下とリポジトリ配下のどちらでも資産を参照できるようにする。
- ViteからManus専用のデバッグ収集、保存プロキシ、実行時プラグイン接続を外す。未設定の解析スクリプトも入口HTMLから外す。
- SVGファイルの存在・形式、外部保存先への参照がないこと、入口とビルド設定の相対パスをCIで監査する。

実機のiPhone Safariでの画像表示、音声解除、FPS、画面回転は、ユーザーによる公開先確認が必要なため未検証として残す。


## PR16実装記録

PR16では、マージ後の公開をiPhoneから再現できるように、GitHub Pagesの手動公開とビルド成果物検査を追加する。

- `Deploy GitHub Pages` workflowを手動実行方式で追加し、mainのビルド成果物をPagesへ公開できるようにする。
- Pages公開前に、ソース契約監査、ビルド、配信対象のindex.html・JavaScript・9つのSVG資産の存在検査を行う。
- Manus実行時プラグイン、デバッグ収集、外部保存先の残存依存を削除し、pnpmの依存ロックも更新する。
- GitHub Pagesの設定とiPhone Safariでの確認手順をREADMEへ記録する。

Pages設定後の実URLでの起動、音声解除、画面回転、FPS、長時間プレイはユーザーによる実機確認が必要なため未検証として残す。


## PR17: 起動失敗時の復旧導線

- `createGameScene` の失敗を未処理Promiseのまま残さず、起動エラー画面へ切り替える
- iPhone Safariから再読み込みできるボタンを追加
- ブラウザ契約監査へ起動失敗処理・alert・再読み込み導線を追加
- 実機Safariの起動失敗再現、縦横表示、Pages公開後の実URLは別途確認する


## PR18: 終了・再戦時の攻撃予告クリーンアップ

- 敗北、相打ち、勝利の終了処理で、表示中の敵攻撃予告を解除する
- 再戦や盤面再初期化の前に、予告状態と対象マスを空にする
- 攻撃予告の解除イベントが二重に発火せず、相打ち終了後も再戦できることを単体検査する
- 実機Safariでの終了画面から再戦時の予告表示は別途確認する


## PR19: 長押し移動の入力修正

- iPhoneの十字ボタンを押し続けると、1マス移動後に一定間隔で移動を続ける
- キーボードの矢印キーとWASDにも同じ初回待ち・繰り返し間隔を適用する
- 二本目のタッチや別方向の同時キーで、移動処理が二重に走らないようにする
- ポインター終了、フォーカス喪失、画面非表示、シーン破棄で繰り返しを停止する
- 実機Safariでの長押し移動、画面端・穴・敵・障害物との接触感は別途確認する


## PR20: 近接カードの実戦経路接続

- 近接斬、横薙ぎ、突進斬、格子断、月光剣を、カード送信から共通の近接段階処理へ接続する
- 近接カードの準備・発動・回復を固定更新で進め、各カードの範囲と多段判定を実ダメージへ反映する
- 突進斬と三重月断が敵陣の手前へ一時的に踏み込めるようにし、攻撃後は元の安全位置へ帰還する
- 近接5枚の実送信と、敵陣への一時踏み込み・帰還を単体検査で固定する
- 実機Safariでの近接入力、敵陣踏み込み時の視認性、障害物・穴・同時攻撃との接触感は別途確認する


## PR21: カード対象と実処理の契約統一

- 最寄り敵の選択を、範囲プレビューと実戦処理で同じマンハッタン距離・列・行の順序へ統一する
- 時限地雷、拘束杭、毒霧装置、引力場は、敵陣の空きパネルから実際に選ばれる設置先を範囲プレビューにも反映する
- 強襲転送は、占有・障害物・穴を避けて実際に移動できる転送先をプレビューと実処理で共有する
- 逆位相穴は、空き3マスへの穴化と使用中の敵マスへの破砕ダメージを同じ対象一覧へ反映する
- 移動後のプレイヤー位置を、カード範囲盤のプレイヤー表示にも反映する
- 実機Safariでのカード選択中の対象表示、転送先、設置先の視認性は別途確認する


## PR22: 無敵付与時間の統一

- 強襲転送と位相迷彩の位相化時間を、`COMBAT_BALANCE.phase.invincibilityMs`の5秒へ統一する
- カード定義の説明・時間メタデータも5秒へ合わせ、HUDの残り時間表示と実処理の解除境界を一致させる
- 夢幻障壁は完全無敵ではなく、100未満の攻撃だけを無効化する独立した障壁として扱う
- 299回の固定更新では位相化を維持し、5秒ちょうどの更新で解除される回帰検査を追加する
- 実機Safariでの位相化中の残り時間表示、解除直後の被弾表示は別途確認する


## PR23: 近接カードの対象範囲と表示契約

- 近接斬、横薙ぎ、突進斬、格子断、月光剣のカード説明へ、実際に命中する対象マスを明記する
- カード範囲ラベルを、正面1マス・正面縦3マス・最寄り敵1マス・最寄り敵中心十字・正面同一行2マスへ分離する
- カスタム画面のプレビュー遅延と戦闘中の方向ガイドを、固定列ではなく現在の対象タイルから算出する
- 近接5枚の範囲ラベルとプレビュータイルを単体検査で固定する
- 実機Safariでのカード説明、対象タイル、格子断の敵位置追従は別途確認する


## PR24: 通常弾の終点と近接専用モーションの検証

- 通常・チャージ射撃は同じ行の敵を表示終点にし、敵不在時は敵領域最奥まで直線弾を表示する
- `?vfx=<cardId>&demo` の近接5枚も共通対象計算から正確な対象タイルを表示する
- 既存のカードID別切断アークを5枚すべてへ接続した状態を監査・回帰検査する
- 敵不在の通常・チャージ射撃と対象敵ありの表示終点を単体検査で固定する
- 実機Safariで敵撃破後の直線弾、5枚の斬撃アーク視認性は別途確認する
