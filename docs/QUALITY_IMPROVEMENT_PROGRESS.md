# ゲーム品質改善の進捗 — 2026-09-11

2026-09-08 の Astra High 監査・改善計画に従う。監査基準の main は `8275440178f6c7fe955b832c11eec093081d43bb`（PR6マージ済み）。

## 今回の実装：計画の PR8（Draft予定）

監査対象：結果画面でのランキング通信失敗からの復帰。失敗を結果表示と分離して明示し、再試行ボタンから同じ結果を再送信できることを受け入れ条件とした。

| 変更前の不整合 | 改善後の結果 | 確認状況 |
| --- | --- | --- |
| ランキング登録に失敗すると、結果は見えていても再送信する操作がなく、再読み込みや再挑戦が必要になる | 「ランキングを再試行」を結果画面へ表示し、同じ結果キーの送信済み状態を解除して再登録・再取得を実行 | 型検査、全テスト、ブラウザ契約監査で確認 |
| ランキングの通信失敗が一度起きると、同じ結果画面での復旧経路を自動検査できない | 再試行コールバックと再実行トークンをブラウザ契約監査で固定し、結果画面の導線が欠落しないようにした | 結果画面テスト、ブラウザ契約監査で確認 |

実機未確認：iPhone 17 Pro Safariで通信失敗後に再試行ボタンが読めること、再試行中の状態表示、成功後にボタンが消えること。

## 今回の実装：計画の PR7（Draft予定）

監査対象：通常モードのフォルダから提示する5枚の選出と、Wave・再選択ごとの変化。カード名が重複するフォルダでも物理カードIDを一意性の基準にし、同じ5枚組が続けて提示されないこと、seed付き実行では同じ順序を再現できることを受け入れ条件とした。

| 変更前の不整合 | 改善後の結果 | 確認状況 |
| --- | --- | --- |
| 30枚フォルダからの手札は抽選されるが、同じ物理5枚組が再提示されても検知・保証できない | `instanceId`を正規化した提示履歴をデッキが保持し、同一Wave内の連続提示を避ける。各手札内の物理カード重複も回帰検査 | フォルダテスト、GameWorldテストで確認 |
| Waveリセット時に直前Waveの初回手札と同じ組み合わせになる可能性をUI・テストで確認できない | 直前に提示した5枚組を次Waveの初回候補から除外し、Wave内の提示番号をスナップショットへ公開 | Wave遷移回帰テスト、表示確認で確認 |
| seed付きデッキは再現できても、通常実行の過負荷カード抽選が固定乱数列で、runごとの変化が弱い | 過負荷抽選もrun seedから派生した乱数列へ統合。seed付き実行はWave手札と過負荷列を再現し、通常実行は変化 | GameWorld回帰テストで確認 |
| 画面上は「手札 05 / 05」だけで、再選択が新しい提示なのか判断しにくい | 「提示 01 / 手札 05 / 05」の提示番号を追加し、Wave内のカード選出の変化を可視化 | 型検査、全テスト、ブラウザ契約監査で確認 |

実機未確認：iPhone 17 Pro Safariでの提示番号の折り返し、過負荷状態の短縮手札、Wave遷移直後のカード面の視認性。

## 前回の実装：計画の PR6（mainへマージ済み、PR #51）

監査対象：練習7段階の補給、成功条件、段階のやり直し。練習を「敵を倒すだけ」のデモにせず、段階ごとの学習行動を満たした時だけ次へ進める。

| 変更前の不整合 | 改善後の結果 | 確認状況 |
| --- | --- | --- |
| 全段階が同じ手札で、段階の教材と戦闘入力が一致しない | 段階ごとに固定したカード補給を生成し、補給名を練習画面へ表示。通常フォルダを消費しない | 練習定義監査、GameWorld回帰テストで確認 |
| 敵を全滅させるだけで段階クリアになり、移動・予告・接続・カウンター等を実行しなくても進める | 移動、通常射撃、同一コード連続送信、予告確認、カウンター、地形、精神状態、過負荷の進捗を記録し、敵全滅と全目標の完了をクリア条件に統一 | 202テスト、型検査で確認 |
| 被撃破時の再挑戦は無言で、補給・HP回復・進捗リセットが画面から判別できない | 被撃破時と手動の「この段階をやり直す」で、HP・敵・カード・進捗を初期化。やり直し回数と補給済みメッセージを表示 | GameWorld回帰テストで確認 |

実機未確認：iPhone 17 Pro Safariでの練習オーバーレイ、補給名の折り返し、3ボタン配置、モバイル操作中のやり直し操作。

## 前回の実装：計画の PR5（mainへマージ済み）

監査対象：防御・属性・カード効果の実処理と、カード面に表示する対象範囲・説明の整合性。

| 変更前の不整合 | 改善後の結果 | 確認状況 |
| --- | --- | --- |
| 氷壁弾は投擲ダメージだけで、仕様にある耐久70の障害物が残らない | 着弾後、敵陣の空きマスへ固体判定の氷壁を設置。着弾マスが埋まっていても近傍の空きマスへ決定的に退避 | GameWorld回帰テスト、全テスト、型検査で確認 |
| 乱流壁が敵・設置物を押すだけで、仕様にある障壁半減が未実装 | 使用時に生存中の敵障壁を半分（端数切り捨て）にしてから、敵と押し出し可能な設置物を後退 | GameWorld回帰テストで確認 |
| 氷壁弾・樹網・設置・地形・上位カードの表示対象が汎用形状名とずれる | 「最寄り敵中心の2×2」「敵陣全行」「全自陣」など、実処理に合わせた固有ラベルと説明へ統一 | 表示メタデータ回帰テストで確認 |
| 戦闘プロファイルのマージ時にカード固有の範囲ラベルが消える | プロファイルを優先しつつ、未定義ならカード定義のラベルを保持 | 全カード表示テストで確認 |

実機未確認：iPhone 17 Pro Safariでの氷壁の視認性、障害物越しの射撃・移動、乱流壁の押し出しアニメーション。

## 前回の実装：計画の PR4（mainへマージ済み）

監査対象：カード（武器）の表示、選択状態、効果説明。戦闘ルール・威力・接続判定は対象外。

| 変更前の再現 | 改善後の結果 | 確認状況 |
| --- | --- | --- |
| カード名・説明・数値が同じ小さな面に混在し、0威力の防御・回復カードも`OUT`と表示 | シグネチャ、役割、対象範囲、効果値、回数、状態、時間を分離表示。補助カードは「補助効果」など意味のあるラベルに変更 | 全50カードの表示ヘルパーテスト、型検査、ビルドで確認 |
| 選択中の順番と、追加できない理由がカード面からわかりにくい | `選択中・1枚目`、`追加できます`、`追加不可：接続条件…`を表示し、aria説明にも反映 | 既存選択ルールを変更せず、表示契約と全テストで確認 |
| フォーカス中のカードが未選択でも「選択中」と説明される | 未選択なら「タップで追加」、選択中なら「もう一度タップで解除」と表示 | 表示ロジックを型検査・表示テストで確認 |
| 戦闘中の次カードと範囲プレビューの説明が短く、作用対象を読み取りにくい | カード固有シグネチャと「Pから正面の直線へ作用。赤く光るマスが対象」を表示 | ブラウザ契約監査・ビルドで確認 |

| 対象 | 変更 | 確認状況 |
| --- | --- | --- |
| カード表示 | 50枚の演出レシピを選択画面・キューへ反映。内部向け時間・耐久表記をプレイヤー向けに変換 | mainへマージ済み。30ファイル / 195テスト、カード表示ヘルパー監査通過 |
| カード選択 | 選択順・追加可否・接続条件の理由を表示。既存の同名・コード・`*`・最大5枚ルールは変更なし | mainへマージ済み。選択ルール回帰、ブラウザ契約監査通過 |
| 説明・範囲 | 効果値・対象範囲・状態・時間を分離し、範囲プレビューの文章を簡潔化 | mainへマージ済み。TypeScript、Vite、build-output監査通過 |

実機未確認：iPhone 17 Pro Safariの縦横画面、safe-area、5枚表示、長文説明、選択順、範囲プレビュー。

## 前回の実装：計画の PR3

| 対象 | 変更 | 確認状況 |
| --- | --- | --- |
| 移動と溜めの同時操作 | タッチ入力を移動指・溜め指・単発操作に分離し、溜めを押したまま別の指で移動できるようにした。同じ種類の二本目と、射撃・カード操作との競合は従来どおり拒否 | mainへマージ済み。8件の入力ガード回帰テスト、全テストで確認 |
| カードボタン・射撃受付 | カード選択の1タップ、通常攻撃・チャージ・カード/CUSTOMの pointerdown とアクセシブルなラベルをブラウザ契約監査で固定 | mainへマージ済み。ブラウザ契約監査通過 |
| 横画面の操作領域 | coarse pointer の横画面で十字キーとアクションボタンを44px以上に統一 | mainへマージ済み。実機での見え方確認待ち |

戦闘ルール、威力、カードデータ、敵データ、ランキング処理は変更していない。今回の変更はスマートフォンの入力受付と操作領域に限定した。

## 前回の実装：計画の PR2

| 対象 | 変更 | 確認状況 |
| --- | --- | --- |
| Q02：表示弾と実判定のずれ | 表示側がイベントの独自速度・独自終点を持たず、`ProjectileState` の `position`・`direction`・`travelProgress`・`activeAt`・`flightMs`・`target` から毎フレーム位置を再計算 | mainへマージ済み。自動テスト・NullEngineシーン接続・ブラウザ契約監査で確認。実機での見え方確認待ち |
| Q03：HOPPER着地範囲のずれ | 予告時に固定した着地点を着地処理まで使う回帰テストを追加。予告後にプレイヤーが移動しても、移動先には着地ダメージを出さないことを確認 | mainへマージ済み。GameWorld回帰テスト通過 |
| Q15：一時弾の後始末 | 表示弾を一時リソースとして追跡し、弾の消滅・Wave遷移・再挑戦・シーン破棄でメッシュと材質を解放 | mainへマージ済み。既存の再挑戦10回シーンテストと全テストで確認 |

表示だけを更新し、HP・威力・衝突・カード・遭遇データのルールは変更していない。`ProjectileSystem` が持つ権威状態を描画へ接続したため、追尾・反射・周回・波動・遅延発射を同じ状態から表示できる。

## PR1から引き継いだ内容

| 対象 | 変更 | 確認状況 |
| --- | --- | --- |
| Q01：11種の敵・ボスの本体欠落 | 全16種を型で網羅する描画定義と、出現・退場に応じた生成・破棄を追加 | 自動テスト通過。実機での見やすさ確認待ち |
| Q15：材質と表示履歴が残る | エフェクト・弾・予告の材質所有権を管理。設置物は消滅時に破棄。同じIDでも種類・所有者・衝突種別が変われば再生成 | 10回の再挑戦で資源数が基準値へ戻ることを確認 |
| Q18の起動部分 | 読み込み・名前入力・失敗・ゲームUIを排他表示。初回描画成功後に操作開始。失敗段階とエラー種別を表示 | 失敗・破棄・初回描画・画面排他テスト通過。版番号表示はPR8へ |

敵は、従来5種も含め、簡単な立体の組み合わせで描画する。盾・砲身・刃・支援柱・周回リングなど、色以外の形の違いを設けた。プレイヤーの既存画像は継続利用。敵には足元リング、攻撃準備・発動・麻痺の姿勢と発光、被弾時の基本反応がある。

敵のHP・攻撃内容・間隔、カードの威力、編成、得点などの戦闘データと GameWorld のルールは変更していない。PR2では、その権威状態を表示側へ接続した。

## 検証

- `vitest run`：31ファイル、208テスト通過（PR7で通常編成の提示履歴・Wave変化・run seedの回帰テスト4件、PR8で結果画面のランキング再試行表示テスト2件を追加）。
- `tsc --noEmit`：通過。
- `vite build`、server bundle、`audit-build-output`：通過。主JSは約1,795 kB（gzip約463 kB）。既存の500 kB超警告は残る。
- 戦闘・カード・編成・盤面・フォルダ・ブラウザ契約の既存9監査：通過。
- 全16種の登録一致、実メッシュ生成、行動姿勢、破棄・再生成を検査。未登録IDは明示的なエラーになる。
- Wave 1〜4のGameWorldと実シーンの接続を検査。
- 実シーンで射撃・カード使用・敵予兆を含む戦闘と再挑戦を10回行い、メッシュ・材質・TransformNode数が基準値へ戻ることを確認。
- 共有材質の最終利用者が消えるまで材質を保持すること、設置物のID再利用時に見た目を更新することを検査。
- WebGL生成不可、シーン生成失敗、初回描画失敗、非同期生成中の画面破棄、名前入力中のEnterを検査。

描画テストは Babylon NullEngine を使用し、実際の形状・材質・ノードを扱う。画像のブラウザデコードのみ省略している。GPU描画、Safari、タッチ操作、フレームレート、実機メモリの確認を意味しない。ブラウザ確認環境からローカルプレビューへの接続が遮断されているため、今回の変更画面の目視確認は未完了。

## 公開前に残る確認

1. レビュー用プレビューで Wave 2、Wave 3、4種のボスを表示。敵の位置・向きがわかり、予告と盤面の境界を隠していないことを確認する。
2. iPhone 17 Pro の Safari で縦横表示、敵撃破・次Wave・再挑戦、起動・再読み込みを確認する。
3. 立体に変更した従来5種と新11種の見た目、発光の強さ、端末負荷を確認し、必要なら形状・材質だけ調整する。

## 後続の計画

| 順番 | 対応内容 | 状態 |
| --- | --- | --- |
| PR1 | 全敵描画・表示物の後始末・起動整理 | mainへマージ済み。実機確認待ち |
| PR2 | 実際の弾と表示の同期、HOPPERなどの予告範囲 | mainへマージ済み。実機確認待ち |
| PR3 | 移動と溜めの同時操作、カードボタン、射撃受付、横画面 | mainへマージ済み。実機確認待ち |
| PR4 | 武器カードの表示・選択・説明 | mainへマージ済み。実機確認待ち |
| PR5 | 防御・属性・カード効果の不整合 | mainへマージ済み（PR #50） |
| PR6 | 練習7段階の補給・成功条件・やり直し | mainへマージ済み（PR #51） |
| PR7 | 通常編成の選出と変化 | ユーザーのマージ報告済み。取得したorigin/mainはPR6のままのため、GitHub上の反映を要確認 |
| PR8 | フォルダ・結果・ランキングの改善 | このブランチ。Draftレビュー待ち |
| PR9 | 公開版の識別、検証・公開手順の整備 | 未着手 |

このPRはDraftとしてレビューへ進める。mainへの取り込みと公開は、この実装作業には含めない。
